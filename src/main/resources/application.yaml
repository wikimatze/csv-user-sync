spring:
  application:
    name: csv-user-sync
  datasource:
    url: jdbc:postgresql://localhost:5432/mydatabase
    password: secret
    username: myuser
  flyway:
    baseline-on-migrate: true # make it explicit
